# Backstage catalog entry, read by paved's scorecard plugin.
#
# Every check paved scores is derived from this file alone — no cluster
# access, no CI credentials. What is absent here is absent from the score.
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: terraform-helm-opencost
  description: >-
      Terraform module to deploy OpenCost (cloud cost monitoring) on Kubernetes using Helm.
  annotations:
    github.com/project-slug: fabiocicerchia/terraform-helm-opencost
    backstage.io/source-location: url:https://github.com/fabiocicerchia/terraform-helm-opencost/
  links:
    - url: https://github.com/fabiocicerchia/terraform-helm-opencost
      title: Source
spec:
  type: library
  lifecycle: production
  owner: group:default/kodebeat
  system: oss-tools
